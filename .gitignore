# dependencies
node_modules/
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# testing
/coverage

# env & secrets — never commit funded keys (brief L5)
.env
.env.*
!.env.example
*.key
*.seed
*.pem

# prisma / sqlite
*.db
*.db-journal

# vendored SDK: only nodejs/kaspa (the pinned file: dependency) is committed.
# Everything else from kaspa-wasm32-sdk-v2.0.1.zip stays local; re-extract the
# zip (sha256 recorded in PROGRESS.md) to restore docs/examples/web variants.
vendor/*.zip
vendor/kaspa-wasm32-sdk/docs/
vendor/kaspa-wasm32-sdk/examples/
vendor/kaspa-wasm32-sdk/web/
vendor/kaspa-wasm32-sdk/nodejs/kaspa-dev/

# spike throwaway testnet keys + state (brief L5: never commit funded keys)
spike/.keys.json
spike/.asks.json

# misc
.DS_Store
*.log
npm-debug.log*
.vercel
